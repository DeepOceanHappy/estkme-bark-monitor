services:
  estkme-bark-monitor:
    build: .
    container_name: estkme-bark-monitor
    restart: unless-stopped
    env_file:
      - .env
    environment:
      ESTKME_HOST: 0.0.0.0
      ESTKME_DATA_DIR: /data
    ports:
      - "127.0.0.1:8765:8765"
    volumes:
      - ./data:/data
